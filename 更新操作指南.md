# 个人笔记网站更新与维护指南

本文档介绍了如何更新本地的 Markdown 笔记，并将其自动同步到部署在 GitHub Pages 上的个人笔记网站。

## 1. 目录结构说明

您的本地笔记目录即为网站的源文件目录：

- 各分类文件夹（如 `Languages/`、`Chips/`、`Linux_note/` 等）：存放具体的 Markdown 笔记。其中 `Languages/` 放代码语言类笔记（C、Lua、Python），`Chips/` 放芯片类笔记（Beken、GD32、JIELI、PHY、PY32、STM32、WISE）。
- `index.md`：网站的首页内容。
- `.vitepress/`：网站的构建配置文件。**请勿将笔记放入此文件夹**。
- `package.json` 等：依赖配置文件。

## 2. 笔记编写规范

1. **新建笔记**：在对应的分类文件夹中新建 `.md` 格式文件。网站侧边栏会自动读取分类文件夹并生成目录。
2. **图片引用**：
   - 推荐使用相对路径引入图片。例如：`![alt text](./image.png)`
   - 请尽量避免在图片文件名中使用 `<` 或 `>` 等特殊字符。
   - 兼容常规 Markdown 图片语法。
3. **内容修改**：直接使用任意 Markdown 编辑器（如 VS Code、Obsidian、Typora）修改并保存文件。

## 3. 同步到网站（自动化部署流程）

本网站已配置 GitHub Actions 自动化部署流程，您只需将本地修改提交并推送到 GitHub 仓库，网站将会在 1-3 分钟内自动完成构建和更新。

### 操作步骤

1. **查看更改**（可选）：
   在终端运行 `git status` 查看修改过的文件。
   
2. **添加到暂存区**：
   ```bash
   git add .
   ```

3. **提交更改**：
   ```bash
   git commit -m "docs: 更新部分笔记内容"
   ```
   > 提示：提交信息可以根据实际修改内容自定义。

4. **推送到 GitHub**：
   ```bash
   git push origin main
   ```
   > 注意：如果您的默认主分支为 `master`，请使用 `git push origin master`。

### 验证部署

推送完成后：
1. 访问您的 GitHub 仓库页面，点击顶部的 **Actions** 标签卡。
2. 您将看到名为 "Deploy VitePress site to Pages" 的工作流正在运行。
3. 等待其显示绿色的打勾标志，即可访问您的 GitHub Pages 网址，查看更新后的笔记内容。

## 4. 本地预览网站（可选）

如果您希望在推送前在本地预览网站效果：

1. 打开终端，进入笔记根目录。
2. 运行本地预览命令：
   ```bash
   npm run docs:dev
   ```
3. 在浏览器中打开提示的本地地址（通常是 `http://localhost:5173/`）。
4. 预览无误后，按下 `Ctrl + C` 关闭本地服务器。

## 5. 网站基础配置（GitHub Pages）

若要确保网站可被公网访问，请在 GitHub 仓库进行如下设置：
1. 进入 GitHub 仓库的 **Settings** -> **Pages**。
2. 在 **Build and deployment** 下的 Source 选项中，选择 **GitHub Actions**。
3. （可选）在 **Custom domain** 中绑定您的自定义域名，并在您的域名解析服务商处添加 CNAME 记录指向 `<您的用户名>.github.io`。
